#!/bin/sh
# Nivik git policy (see .githooks/policy.json). Logic lives in lib/run.mjs.
# git passes the ref updates on stdin; run.mjs reads them.
dir=$(cd "$(dirname "$0")" && pwd)
exec node "$dir/lib/run.mjs" pre-push "$@"
